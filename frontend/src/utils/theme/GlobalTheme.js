import { createTheme } from '@mui/material/styles';


const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#717dc0',
    },
    secondary: {
      main: '#f50057',
    },
  },
});


export default theme;